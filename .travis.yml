language: ruby
rvm:
- 2.5.5
install:
- docker pull lambci/lambda:build-ruby2.5
script:
- make plan
deploy:
- provider: script
  script: make apply
  skip_cleanup: true
  on:
    repo: brutalismbot/brutalismbot
    tags: true
